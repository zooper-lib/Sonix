# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.buildlog/
.history
.svn/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# VS Code and project-specific files
# Note: .vscode/settings.json and .kiro/ files are checked in for the project
# Do not add .vscode/ or .kiro/ patterns here

# Flutter/Dart/Pub related
# Libraries should not include pubspec.lock, per https://dart.dev/guides/libraries/private-files#pubspeclock.
/pubspec.lock
**/doc/api/
.dart_tool/
.flutter-plugins-dependencies
/build/
/coverage/

# Generated test files (too large for version control)
test/assets/generated/

# Real audio files for integration testing (user-provided, may be copyrighted)
test/assets/real_audio/
!test/assets/real_audio/README.md

native/windows/*
test/assets/ffmpeg/*

# FFMPEG binaries and build artifacts should never be committed
# Users must download their own FFMPEG binaries using the provided tools

# FFMPEG binary directories (all platforms)
build/ffmpeg/
test/fixtures/ffmpeg/

# FFMPEG library files (Windows)
avcodec-*.dll
avdevice-*.dll
avfilter-*.dll
avformat-*.dll
avutil-*.dll
swresample-*.dll
swscale-*.dll
postproc-*.dll

# FFMPEG library files (Linux)
libavcodec.so*
libavformat.so*
libavutil.so*
libswresample.so*
libswscale.so*
libavfilter.so*
libavdevice.so*

# FFMPEG library files (macOS)
libavcodec.dylib*
libavformat.dylib*
libavutil.dylib*
libswresample.dylib*
libswscale.dylib*
libavfilter.dylib*
libavdevice.dylib*

# Native library build directories (temporary build artifacts)
build/sonix/

# But allow our native libraries in platform directories (these are our code)
# linux/libsonix_native.so - ALLOWED
# windows/sonix_native.dll - ALLOWED  
# macos/libsonix_native.dylib - ALLOWED

# FFmpeg libraries in Flutter bundles (deployed automatically, but not ours)
**/bundle/lib/libav*.so*
**/bundle/lib/libsw*.so*
**/bundle/lib/avcodec-*.dll
**/bundle/lib/avformat-*.dll
**/bundle/lib/avutil-*.dll
**/bundle/lib/swresample-*.dll